{
  "name": "express-mock-api",
  "version": "1.0.0",
  "description": "A mock api to deliver JSON content from a local folder to simulate a similar REST api path pattern. This mock api can be useful for getting projects up and running on mock data.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "David Becker",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.16.1",
    "cors": "^2.8.1",
    "express": "^4.14.0",
    "multiparty": "^4.1.3"
  },
  "devDependencies": {
    "express-handlebars": "^3.0.0",
    "fs": "^0.0.1-security",
    "path": "^0.12.7"
  }
}
